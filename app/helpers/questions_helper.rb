module QuestionsHelper
	# def is_checked_for(user,option)
 #  	answers = @questions.first.answers.where(test_id: @test.id, user_id: current_user)
	# 	if answers.present?  
 #      answers.first.answer_options.first.option_id == option.id
 #    else 
	# 		false 
	# 	end
	# end
end
